import { getStore } from '@netlify/blobs';
const store=getStore({name:'my-workspace-data',region:'eu-central-1'}),KEY='workspace.json';
const seed={projects:[{id:'p1',name:'Allgemein',description:'Zentrale Aufgaben'},{id:'p2',name:'Website',description:'Website-Projekt'}],tasks:[{id:'t1',title:'Workspace einrichten',status:'done',project:'Allgemein'},{id:'t2',title:'Mobile Ansicht testen',status:'doing',project:'Allgemein'},{id:'t3',title:'Neue Projektidee sammeln',status:'todo',project:'Allgemein'},{id:'t4',title:'Startseite verbessern',status:'backlog',project:'Website'}],notes:[{id:'n1',title:'Willkommen',body:'Deine zentrale Workspace-Notiz.'}],messages:[]};
const res=(x,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{'content-type':'application/json','cache-control':'no-store,no-cache,must-revalidate,proxy-revalidate','pragma':'no-cache','expires':'0'}});
export default async req=>{try{if(req.method==='GET'){let x=await store.get(KEY,{type:'json',consistency:'strong'});if(!x){x=seed;await store.setJSON(KEY,x)}return res(x)}if(req.method==='PUT'){let x=await req.json();if(!x?.tasks||!x?.projects)return res({error:'invalid'},400);await store.setJSON(KEY,x);return res(x)}return res({error:'method'},405)}catch(e){console.error(e);return res({error:'storage'},500)}};
