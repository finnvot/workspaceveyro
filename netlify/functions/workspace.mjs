import {getStore} from '@netlify/blobs';const s=getStore({name:'my-workspace-data',region:'eu-central-1'}),k='workspace.json';const seed={projects:[{id:'p1',name:'Allgemein',description:'Zentrale Aufgaben'}],tasks:[],content:[],bugs:[],notes:[{id:'n1',title:'Willkommen',body:''}],messages:[]};const out=(x,n=200)=>new Response(JSON.stringify(x),{status:n,headers:{'content-type':'application/json','cache-control':'no-store'}});export default async r=>{try{if(r.method==='GET'){let x=await s.get(k,{type:'json',consistency:'strong'});if(!x){x=seed;await s.setJSON(k,x)}return out(x)}if(r.method==='PUT'){let x=await r.json();x.content??=[];x.bugs??=[];await s.setJSON(k,x);return out(x)}return out({error:'method'},405)}catch(e){return out({error:'storage'},500)}};
